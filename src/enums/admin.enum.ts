export enum AdminStatusEnum {
  active = 'active',
  pending = 'pending',
  blocked = 'blocked',
  disabled = 'disabled',
}

export enum AdminAccessLevelEnum {
  full = 'full',
  restricted = 'restricted',
}
